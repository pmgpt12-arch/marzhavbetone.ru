# PROJECT STATE

Обновлено 2026-08-15. Ветка выкладки — main; текущий коммит берётся из git, а не
отсюда. Индекс состояния, не журнал: число без файла-источника — дефект.
Предел 40 строк. Обновляется вместе с законченной единицей работы.

**Прод:** marzhavbetone.ru, 82 адреса в `sitemap.xml`. Выкладка — `deploy.yml`
на push в main и ежедневно в 09:00 МСК.

## Текущий приоритет

Сбор поискового спроса работает: `data/seo/search-demand.csv` в main —
8 запросов с показами, прогон 31879408413 (15.08), дальше собирается сам
по понедельникам. F1-05 и D-01 закрыты. Приоритет — F2-04 и разбор
собранного спроса.

## В работе

- **F2-04** P0 — покупка привязана к каналу и странице. Реализация проверена
  15.08, дубль purchase на повторном заходе закрыт; приёмка ждёт первой продажи
- **F3-01** P0 — разблокирована 15.08: база непуста, но приёмка «≥50 строк
  с показами» — в базе 8, спрос копится еженедельными прогонами

## Блокеры текущей работы

Только связанные с приоритетом и задачами выше; полный список — в бэклоге.

- **F3-01** ждёт накопления — 8 строк из ≥50 по приёмке
- **C-05** ждёт F2-04 — success.html без предложения следующего комплекта
- **C-08** ждёт F2-04 — разбор недели не начинается с заказов
- **F1-06** решение владельца — второго источника спроса нет, ни секрета, ни сборщика

## Дальше

Список считается командой, наизусть не ведётся: `python3 tools/check_backlog.py --next`

## Источники

- Задачи — `data/ops/backlog.csv`; план — `docs/EXECUTION-90.md`
- Числа сайта — `content/metrics/`, последний снимок `site-2026-08-14.yaml`
- Касса — `docs/CURRENT_CHECKOUT_FLOW.md`; спрос — `data/seo/search-demand.csv`
