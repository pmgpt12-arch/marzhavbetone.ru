# Тексты для Дзена

**Трансляция из `rss.xml` подключена — ответ владельца 16.08.2026.** Порядок,
описанный ниже, пройден до конца: порог подписчиков взят, лента принята после
отказа 06.08 и повторной подачи 07.08. Ручная публикация копированием в Студию
больше не нужна: статья, попавшая в `articles/`, уезжает в канал сама на
ближайшем деплое.

Что из этого следует для работы:

- **Новый материал руками в Студию не носят.** Он появляется в ленте
  пересборкой фида — `deploy.yml`, ежедневно в 09:00 МСК.
- **Заголовок правится в заготовке**, а не в Студии: с 16.08.2026 фид берёт
  строку из блока «Заголовок», и правка в Студии разойдётся с лентой.
- **Заготовки остаются нужны** — ради заголовка и ради текста без таблиц.
  Инструкция «как публиковать с телефона» внутри них теперь историческая.

Чего подключение не означает. Лента подключена по Студии, со слов владельца;
**сколько статей она реально привезла — не измерялось ни разу.** Это разные
события, и второе меряется прогоном:

```bash
python3 tools/dzen_channel.py --snapshot   # из Actions: dzen.ru закрыт из сессии
```

Он печатает `feed_delivered` — сколько статей ленты нашлось в канале. Ноль при
подключённой ленте это не «выключено», а повод к повторному снимку: лента могла
ещё не отработать. Прогон — `.github/workflows/dzen-channel.yml`, еженедельно.

Первым снимком проверяется то, что записано в порядке ниже и подтверждения
замером не имеет: нет ли в канале вторых копий двенадцати материалов,
опубликованных руками. Из ленты они исключены списком `PUBLISHED_BY_HAND`, но
исключение делалось до подключения.

## Как сюда пришли

Пока канал не набрал 10 подписчиков, автоматическая трансляция была недоступна —
Дзен включает её только после этого порога. До тех пор публиковали вручную,
копированием в редактор Студии.

Это был не обходной путь, а нормальный первый шаг. Дзен показывает статьи и
неподписчикам, поэтому именно с ручных публикаций набрались первые читатели — и
заодно те подписчики, которых не хватало для RSS.

## Сначала — что уже в канале

Публикации уходят в Дзен вручную, поэтому репозиторий сам по себе не знает,
что в канале уже есть. Перед подготовкой материала:

```bash
python3 tools/dzen_channel.py
```

Скрипт читает публикации канала через публичный API Дзена, печатает их
списком и сверяет заголовки с заготовками в этой папке и статьями сайта.
Точный повтор он останавливает кодом возврата 1. Пересечение тем скрипт
поймать не может — заголовки на сайте короткие и рекламные, поэтому список
печатается целиком, чтобы тему сверил человек.

Так уже случалось: три материала готовились по темам, которые в канале
были разобраны неделей раньше. Публикации делались вручную, в репозитории
следа не оставили, и совпадение обнаружилось только при взгляде на канал.

## Что в канале на 04.08.2026

Двенадцать опубликованных. Список снят с экрана Студии и
сопоставлен с заготовками по заголовку:

| Заготовка | Заголовок в канале | Когда |
|---|---|---|
| материал 01 | Генподрядчик зарабатывает на вас больше, чем на стройке | 7 д |
| `02` | Аванс — это не деньги. Это крючок | 7 д |
| `03` | Банкротство генподрядчика планируется за год до вашего первого акта | 7 д |
| `04` | Скидка 15% на тендере — это не конкуренция. Это отбор жертв | 7 д |
| `05` | Пять доказательств, которые заставят заплатить | 7 д |
| `06` | Подписанная КС-2 не значит, что вам заплатят | 7 д |
| `07` | Смета сошлась. Маржи нет | 3 д |
| `08` | Бетон залит. Доказать объём больше нечем | 3 д |
| `09` | Опасный пункт договора не выглядит опасным | 3 д |
| `15` | «Верните весь аванс». Неотработанный аванс — это разница | 1 д |
| `11` | Вы написали арбитражному управляющему. В реестр это вас не включает | 6 ч |
| `10` | Генподрядчик в банкротстве: претензия и иск больше не работают | 05.08 |

Прежний счёт — девять — вёлся от сверки канала 27.07 и отстал на две
публикации. Отсюда правило: счёт канала записывается с экрана Студии, а не
выводится из того, что заготовки существуют.

Остались неопубликованными: `12`, `13`, `14`, `16`, `17` и двенадцать
новых, `18`–`29`. Всего семнадцать — ровно столько, сколько в фиде.

## Порядок выхода на автотрансляцию

Все заготовки в этой папке сделаны по статьям сайта, а значит, они есть и в
`rss.xml`. Опубликованный вручную материал никуда не денется при включении
трансляции — Дзен привезёт статью из фида второй раз. Поэтому порядок такой:

1. Публикуем вручную и набираем подписчиков.
2. При 10 подписчиках **исключаем опубликованные вручную статьи из фида**.
3. Включаем трансляцию из `rss.xml` — фид завозит остальные сам.

Раньше здесь стояло «удаляем ручные публикации в Студии», и на тот момент это
было единственным выходом: исключение из фида упиралось в требование Дзена
«не меньше 10 материалов», а в фиде было ровно 10. С 01.08.2026 в фиде 28
материалов, и даже за вычетом всех девяти когда-либо опубликованных вручную
остаётся 19. Удалять публикации больше не нужно — исключение из фида
сохраняет накопленные дочитывания и подписчиков, а удаление их сжигало.
Исключение сделано: `PUBLISHED_BY_HAND` в `tools/build_rss.py`, одиннадцать
адресов с причиной. Проверяется `tools/check_rss.py`.

### Дзен отказал 06.08.2026, причина — наша

Ход событий по часам, а не по памяти:

| Когда (МСК) | Что |
|---|---|
| 05.08, 21:47 | В `main` слито посуточное придержание — фид срезан с 18 материалов до 1 |
| 05.08, 21:57 | Подана заявка: «Приняли вашу заявку… проверить разметку ленты — обычно это занимает несколько дней» |
| 06.08, 15:02 | Отказ: «Содержимое RSS ленты не соответствует требованиям Дзена к контенту. Исправьте содержимое и добавьте RSS на проверку снова» |

Прежняя запись в этом разделе утверждала, что порога в десять материалов
нет — раз заявку приняли при одном. **Это было неверно дважды.** «Приняли»
означало «поставили в очередь», а не «проверили». А порог записан у площадки
прямо: «При первой разметке лента должна содержать минимум 10 материалов.
При этом на сайте должно быть размещено не менее 3 публикаций за последний
месяц» — `dzen.ru/help/ru/website/rss-modify.html`.

То есть отказ вызвало наше собственное придержание, слитое за десять минут
до подачи.

### Четыре расхождения с требованиями, снятые замером

Причину отказа площадка не разбирает по пунктам, поэтому сверены все
требования её документации. Разошлись четыре, все исправлены и все закрыты
проверкой в `tools/check_rss.py` — проверка доказана откатом, каждая ловит
свой дефект.

1. **Материалов меньше десяти.** Требование первой разметки. Первые десять
   в `RELEASE` открыты одной датой, хвост из семи идёт посуточно с 08.08 по
   14.08. Темп нужен подключённому каналу, а не приёмной комиссии.
2. **Пространство имён объявлено через `https`,** а в примере площадки
   `http://purl.org/rss/1.0/modules/content/`. Это строка-имя, парсер
   сверяет её посимвольно: по https-варианту `content:encoded` для него не
   существует, и материал уезжает без полного текста. Фид при этом остаётся
   валидным XML — ничего не падает.
3. **Теги `strong` (228 штук) и `em` (2)** вне списка площадки: там `b` и
   `i`. Наш список был написан по памяти. Теперь переводятся, а не
   выкидываются — 228 выделений это термины и суммы, ради которых абзац и
   написан.
4. **UTM в ссылке внутри каждого материала.** Площадка требует адресов «без
   UTM-меток и других параметров», а наш собственный `robots.txt` запрещает
   `/*?utm_` — мы звали Дзен туда, куда сами закрыли вход. Метка снята,
   переходы остаются видны в Метрике по источнику `dzen.ru`: грубее, и это
   осознанная потеря.

### Заявка подана повторно 07.08.2026

Правка доехала: деплой по слиянию PR #115 прошёл в 13:26 UTC успешно, лента
на сайте пересобрана. Владелец подал её на проверку тем же адресом
`https://marzhavbetone.ru/rss.xml`.

Что в ленте на момент подачи — замер по сборке из `main`, а не по живому
адресу (сайт из облачной сессии закрыт прокси):

```bash
python3 tools/build_rss.py   # 10 материалов, придержано до срока 7
python3 tools/check_rss.py   # расхождений 0, exit 0
```

Дальше лента растёт сама: хвост из семи выходит посуточно с 08.08 по 14.08,
`schedule: '0 6 * * *'` в `deploy.yml` пересобирает её без пуша. Пока идёт
проверка, делать ничего не нужно.

**Ответ пришёл: лента подключена** (владелец, 16.08.2026). Отсчёт «+12 недель
после RSS» начат этой датой. Остаётся сверка, ради которой ответа и ждали: нет
ли в канале вторых копий двенадцати опубликованных вручную. Из ленты они
исключены списком `PUBLISHED_BY_HAND`, но исключение делалось до подключения, и
замером не подтверждено — считает их первый снимок канала (`feed_delivered`
против списка исключённых).

### Заголовок из заготовки уходит и в фид — с 16.08.2026

До этой даты блок «Заголовок» доставался только ручной публикации, а фид
брал `og:title`, то есть поисковый заголовок статьи. Замер 16.08.2026: из
семнадцати статей расписания четырнадцать теряли на этом написанный
заголовок. Терялось молча — заголовки фид не проверял.

Разница не косметическая. «Гарантийное удержание: что это такое и сколько
оно стоит субподрядчику» написано под запрос «гарантийное удержание»
4 917 в месяц; «Пять процентов, которые вы больше не увидите» — под ленту,
где заголовок конкурирует за клик, а не за позицию. Обе строки правильные,
каждая на своей площадке.

Побочно это разводит две наши страницы по разным запросам: за «гарантийное
удержание» отвечает статья сайта, она каноническая, а копия в Дзене больше
не встаёт с ней в один запрос.

Связь заготовки со статьёй берётся из блока «Ссылка» — там канонический
адрес, и он есть во всех двадцати восьми заготовках. Обложка для этого не
годится: её нет у трёх, и связь по ней даёт неверные адреса (замер откатом
16.08.2026 — `priemka.html`, `dopraboty.html`).

Что это значит для работы: **заголовок заготовки теперь уходит в ленту, и
править его надо в заготовке.** Нет заготовки — идёт заголовок сайта.
Проверка — `python3 tools/test_dzen_titles.py`, стоит в гейте. Вернуть
поисковые заголовки — снять `USE_DZEN_TITLES` в `tools/build_rss.py`,
правка на одну строку.

## Формат файлов

Каждый материал разбит на блоки, которые копируются по одному:

- **Заголовок** — одна строка;
- **Текст** — весь материал одним куском, без разметки;
- **Подзаголовки** — строки, которые после вставки нужно выделить стилем
  «Заголовок»;
- **Ссылка** — последняя строка публикации, уже с UTM-меткой.

Разметки внутри блока «Текст» нет намеренно: редактор Дзена вставляет
`###` и `**` как обычные символы, и вычищать их пришлось бы прямо в
статье. На телефоне это дольше, чем выделить пять подзаголовков. У блока
в рамке на GitHub есть кнопка копирования — текст переносится целиком,
без выделения пальцем.

## Как публиковать

1. Студия Дзена → **«+»** → «Статья».
2. Вставить заголовок.
3. Загрузить обложку из `assets/` — тот же файл, что на сайте.
4. Вставить текст одним куском.
5. Выделить подзаголовки стилем «Заголовок».
6. Последней строкой вставить ссылку — она уже с UTM-меткой, поэтому
   переходы и продажи из Дзена будут видны отдельно в сводке продаж.
7. Опубликовать.

## Чем текст отличается от статьи на сайте

- **Таблиц нет.** Редактор Дзена работает с ними плохо, а в RSS-фиде они
  и так вырезаются. Всё, что было в таблицах, переписано списками — иначе
  читатель Дзена терял бы половину смысла.
- **Одна ссылка на сайт в конце.** Много исходящих ссылок Дзен не любит.
- **Оговорки сохранены.** Для юридических тем это обязательно, независимо
  от площадки.

## Порядок публикации

Нумерация файлов — по времени написания, не по силе крючка. Порядок выхода
задан ниже отдельно и с ним не совпадает.

### Что репозиторий знает о канале

Материал `01` опубликован и из очереди убран. Это не всё, что в канале, и
порядок ниже собран уже с учётом остального — вот на каком основании.

| Заготовки | Что известно | Откуда |
|---|---|---|
| `02`, `03`, `05`, `06` | тема **уже в канале** | сверка канала 27.07 |
| `04` | тема была свободна 27.07, опубликована сразу после | сверка 27.07 + список 28.07 |
| `07`, `08`, `09` | свободны | владелец подтвердил: список опубликованного — шесть выше |
| `10`–`17` | в канале быть не может | статьи вышли 01.08, заготовки сделаны в тот же день |

Расшифровка. Коммит от 27.07 «Check the Dzen channel before preparing
material» — единственная выполненная сверка с живым каналом. По её итогу
заголовок получил только файл `04`: «тема, которой в канале нет». Остальные
четыре из той партии — `02`, `03`, `05`, `06` — значит, темами в канале уже
были. `content/strategy/README.md` от 28.07 фиксирует шесть материалов
канала: аванс, банкротство генподрядчика, тендер, доказательства
выполнения, КС-2 и заработок генподрядчика — это ровно `01`–`06`, с
тендером, опубликованным в промежутке. К 01.08 счёт вырос до девяти; какие
три добавились, в репозитории не записано, и `07`, `08`, `09` попадают
в этот зазор по времени.

Отсюда правило порядка: **пять заготовок `02`–`06` из очереди выведены**,
три с неизвестным статусом ставятся только после взгляда на канал.

### Первые семь дней — калибровка, по одному в день

Алгоритм Дзена первые дни решает, какой аудитории показывать канал. Поэтому
семь материалов идут подряд, ежедневно, и подобраны так, чтобы за неделю
алгоритм увидел **весь спектр денег субподрядчика**, а не один узел:

| День | Файл | Узел | Статус |
|---|---|---|---|
| 1 | `08-akt-skrytyh-rabot` | исполнительная документация | чисто |
| 2 | `15-neotrabotannyy-avans` | аванс после расторжения | чисто |
| 3 | `11-vklyuchenie-v-reestr` | банкротство | чисто |
| 4 | `09-krasnye-flagi-dogovora` | договор | чисто |
| 5 | `16-vozvrat-avansa` | возврат аванса | чисто |
| 6 | `07-smeta-soshlas-marzhi-net` | смета | чисто |
| 7 | `10-reestr-trebovaniy-kreditorov` | банкротство | чисто |

Первый день — самый крупный узел измеренного ядра: «акт скрытых работ»
27 861 запрос в месяц. Раньше здесь стояла КС-2 как самая широкая боль
ниши, и по силе крючка она сильнее, — но её тема в канале уже разобрана,
а повтор в калибровочном окне стоит дороже разницы в широте.

Все семь дней чистые. Ранее `07`, `08` и `09` стояли под вопросом: счёт
публикаций в канале дорос до девяти, а опознаны были шесть. Владелец
02.08 подтвердил, что список опубликованного — те самые шесть, и вопрос
снят. Если при постановке в отложку в канале всё же обнаружится одна из
этих тем, день не переставляется, а **заменяется ближайшим из очереди
недели 2** — узел важнее файла.

### Выпуск по одной в день — фидом, а не руками

Решение владельца 05.08.2026. Дзен привозит из фида всё, чего у него ещё нет:
лента, отданная целиком, дала бы залп из семнадцати публикаций и сломанную
калибровку. Поэтому фид отдаёт только то, чему пришёл срок.

Расписание — `RELEASE` в `tools/build_rss.py`, даты те же, что в таблице ниже.
Деплой идёт ежедневно в 09:00 МСК (`schedule` в `deploy.yml`), пересобирает
фид, и в ленте появляется ровно одна новая статья. Руками в Студию заходить не
нужно — при включённой трансляции Дзен заберёт её сам.

Чего это стоит и что делать, если упрётся: пока в фиде меньше десяти
материалов, Дзен может не дать подключить ленту — у него требование к
минимуму. Проверка `check_rss` это состояние называет и не считает поломкой,
но если Дзен откажет, надо выпустить первые десять одной датой и продолжить
посуточно. Правка на одну строку в `RELEASE`.

### Порядок выпуска, семнадцать дней

Владелец 04.08 подтвердил ритм: по статье в день, минимум десять дней. Шести
оставшихся заготовок на это не хватало — очередь кончалась 10 августа.
Двенадцать новых собраны из статей сайта, по которым заготовок не было
(`tools/build_dzen_draft.py`), и очередь закрывает семнадцать дней.

Порядок — чередование денежных узлов: два материала одного узла подряд не
ставятся, это держит охват, к которому канал калибруется.

| Дата | Заготовка | Узел |
|---|---|---|
| 05.08 | `16-vozvrat-avansa` | аванс |
| 06.08 | `19-garantiynoe-uderzhanie` | удержания |
| 07.08 | `20-ispolnitelnaya-dokumentaciya` | исполнительная |
| 08.08 | `17-bankovskaya-garantiya-na-avans` | аванс |
| 09.08 | `28-protokol-raznoglasiy` | договор |
| 10.08 | `18-akt-osvidetelstvovaniya` | исполнительная |
| 11.08 | `13-srok-vklyucheniya-v-reestr` | банкротство |
| 12.08 | `26-stroitelnyy-musor` | смета |
| 13.08 | `21-dogovor-subpodryada` | договор |
| 14.08 | `27-vozvrat-uderzhaniya` | удержания |
| 15.08 | `14-zayavlenie-o-vklyuchenii` | банкротство |
| 16.08 | `29-akt-peredachi-ploshchadki` | исполнительная |
| 17.08 | `22-vsyo-vklyucheno` | смета |
| 18.08 | `12-zapros-upravlyayushchemu` | банкротство |
| 19.08 | `24-srok-uderzhaniya` | удержания |
| 20.08 | `25-dopraboty-bez-soglasheniya` | допработы |
| 21.08 | `23-raboty-ne-prinyaty` | приёмка |

Две последние стоят в конце не случайно: статьи `raboty-ne-prinyaty` и
`dopraboty-bez-soglasheniya` короткие — около семисот знаков против трёх-девяти
тысяч у остальных. Для Дзена это слабый материал, и если к тому времени статьи
не дописаны, лучше заменить их новыми темами из ядра, чем публиковать коротышки.

### Прежний план: 2–3 в неделю, четыре оставшихся

Обрыв после ежедневной серии читается алгоритмом как затухание, поэтому
восьмой день пустым быть не должен. Оставшиеся четыре закрывают две недели:

| Неделя | Файлы |
|---|---|
| 2 | `17-bankovskaya-garantiya-na-avans`, `13-srok-vklyucheniya-v-reestr` |
| 3 | `12-zapros-upravlyayushchemu`, `14-zayavlenie-o-vklyuchenii` |

Внутри недели порядок свободный, но два материала одного узла подряд не
ставятся: чередование узлов держит охват, к которому канал откалиброван.
На неделе 3 чередовать нечем — оба файла про банкротство, и третьего узла
в остатке нет. Разносить их по краям недели, а не ставить в соседние дни.

После недели 3 очередь пуста: одиннадцать заготовок против прежних
шестнадцати. Пять ушли не в мусор, а в уже опубликованное, поэтому
следующая порция — не переписывание `02`–`06`, а новые темы из ядра.

### Перед каждой публикацией

Сверить тему с тем, что уже в канале. Скрипт `tools/dzen_channel.py` из
облачной сессии не работает — прокси закрывает API Дзена, — поэтому сверка
глазами. Повтор в калибровочном окне хуже пропущенного дня: канал, который
повторяется, алгоритм показывает реже.

После первых публикаций порог в 10 подписчиков обычно закрывается, и дальше
включается трансляция из фида — вручную публиковать больше не придётся.
